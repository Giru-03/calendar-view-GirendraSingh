import { CalendarView } from './components/Calendar/CalendarView'

function App() {
  return <CalendarView events={[]} onEventAdd={() => {}} onEventUpdate={() => {}} onEventDelete={() => {}} />
}

export default App